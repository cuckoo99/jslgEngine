module('Element');
// 非同期の範囲取得テスト
testSettingAsSync("TestJSlgElementVariable", {
		mainData : {width:10,height:10}
	},
	function(iconController, mainController, connector, options) {
	
	testAsSync('キーの取得', connector, function(name, connector_s) {
	});
});

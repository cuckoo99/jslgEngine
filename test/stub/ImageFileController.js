/*
 * @author cuckoo99
 */

(function() {
	// namespace:
	var o = this.jslgEngine = this.jslgEngine||{};
	o = (o.stub = o.stub||{});

	var ImageFileController = function(data) {
		var dat = data||{};
	};
	
	/**
	 *
	 */
	var p = ImageFileController.prototype;

	p._key = 'Image';
	
	p._list = [];

	p.initialize = function() {	
	}

	p.add = function(connector, data, options) {
	};

	p.getKey = function(key) {
	};

	p.removeAll = function() {
	};

	p.get = function(connector, data, options, callback) {
	};

	p.load = function(connector, data, options) {
	};

	o.ImageFileController = ImageFileController;
}());
